/* eslint-disable no-undef */
// import { initializeApp } from 'firebase/app';
// import { useEffect, useState } from 'react';
// import { getDocs, collection } from 'firebase/firestore'; // Importar Firestore
// import { db } from './firebaseConfig';

// const
// querySnapshot = (database, collectionData) => getDocs(collection(database, collectionData));

// export default querySnapshot;
// import { collection, getDocs } from "firebase/firestore";

// const querySnapshot = await getDocs(collection(db, "users"));
// querySnapshot.forEach((doc) => {
//   console.log(`${doc.id} => ${doc.data()}`);
// });
// const [lunch, setLunch] = useState([]);
// console.log(lunch);
// useEffect(() => {
//   onSnapshot(collection(db, 'lunch'), (snapshot) => {
//     setLunch(snapshot.docs.map((doc) => doc.data()));
//   });
// }, []);
